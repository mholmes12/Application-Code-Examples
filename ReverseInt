/**
 * <p>
 * Reverse Int
 * This progran will output a number with it's digits reversed.
 * </p>
 *
 * @author Megan Holmes
 * @version 1.0, April 15, 2019
 */
import java.util.Arrays;
public class ReverseInt
{
    /**
   * revDigits() will reverse output the final digit in a number and if the number is longer than one digit, remove the last digit and call revDigits() recursivly.
   *  Parameter Name         Type           Purpose
   * <b>n</b>                int            number being reversed
   */
  public static void revDigits (int n)
  {
    System.out.print (n % 10);
    if (n / 10 != 0)
      revDigits (n / 10);
  } 
    /**
   * Main method creates an instance of ReverseInt class and tests the reverseInt method. 
   */
  public static void main (String[] args)
  {
    ReverseInt d = new ReverseInt ();
    d.revDigits (354);     
  }
}


